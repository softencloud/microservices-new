apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: demo-frontend-fast
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 8Gi
  storageClassName: standard